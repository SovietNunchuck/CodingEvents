@model List<CodingEvents.Models.EventTag>

@if (Model.Count == 0)
{
    <h1>No elements to display with the given tag.</h1>
}
else
{
    <h1>Events tagged: @Model[0].Tag.Name</h1>

    <ul>
        @foreach (EventTag eventTag in Model)
        {
            <li>@eventTag.Event.Name</li>
        }
    </ul>
}
